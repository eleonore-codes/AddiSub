export function cardCanvas(card){const canvas=document.createElement('canvas');canvas.width=1080;canvas.height=1080;const c=canvas.getContext('2d');c.fillStyle='#f3f6f8';c.fillRect(0,0,1080,1080);c.fillStyle='#163f59';c.fillRect(60,60,960,960);c.fillStyle='white';c.textAlign='center';c.font='bold 60px Arial';c.fillText('AddiSub',540,170);c.font='36px Arial';c.fillText(card.date.split('-').reverse().join('.'),540,250);c.font='bold 68px Arial';c.fillText(`ERFOLG NR. ${card.number}`,540,385);c.font='bold 96px Arial';c.fillText(`${Math.round(card.score*100)} %`,540,535);c.font='36px Arial';c.fillText('Trainingswertung',540,595);c.fillText(card.phase==='a'?'3-Minuten-Training':'2-Minuten-Training',540,690);c.fillText(`${card.streak} ${card.streak===1?'Tag':'Tage'} in Folge erfolgreich gelernt`,540,790);c.font='bold 44px Arial';c.fillText(card.message,540,910);return canvas;}
export async function shareCard(card){const canvas=cardCanvas(card);const blob=await new Promise(resolve=>canvas.toBlob(resolve,'image/png'));if(!blob)throw Error('Karte nicht verfügbar');const file=new File([blob],`AddiSub-Erfolg-${card.number}.png`,{type:'image/png'});if(navigator.canShare?.({files:[file]})){await navigator.share({files:[file],title:`AddiSub – Erfolg Nr. ${card.number}`});return;}const url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=file.name;a.click();setTimeout(()=>URL.revokeObjectURL(url),10000);}
